<%for user in order user_order.users
    if(current user.id==user.id || current user_id==order_user_id)
        
           link_to "New Order",order_order_items_path
    else
            puts "You can't add items"

          end%>
        

